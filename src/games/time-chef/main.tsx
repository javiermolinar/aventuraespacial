import React from 'react';
import ReactDOM from 'react-dom/client';
import TimeChef from './TimeChef';
import '../../styles/site.css';

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><TimeChef /></React.StrictMode>);
